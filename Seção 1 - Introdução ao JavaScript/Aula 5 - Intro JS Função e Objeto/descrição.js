/**
 * Funções - São Trechos de Códigos que podem ser reutilizados.
 * Classe - Agrupa atributos e métodos no mesmo lugar.
 * return - Encerra a função e retorna um valor.
 * Conhecido como Argumentos ou PARÂMETROS - são informações que as funções precisam.
 * Argumentos ou Args permitem acessar Parâmetros de uma função.
 *
 * call back - Função de retorno como parâmetro após uma ação.
 * DOM (Document Object Model) - a estrutura como o HTML é formado.
 * Eventos - Ações realizadas na aplicação que dispara rotinas.
 * window - Manipulamos a aplicação.
 *
 * Array - Variável indexada. Cuja posições são os índices e os valores são os dados que temos dentro dele.
 *     Armazena mais de uma informação ao mesmo tempo.
 * forEach - Laço para percorrer Array.
 *
 * A classe irá englobar todos os assuntos em comum.
 *     Atributos (variáveis) a minha informação, onde eu armazeno.
 *     Métodos (funções) as ações que devem acontecer.
 * this é um comando interno usado para referenciar.
 * Instância - É quando o objeto representa uma classe.
 * new tira uma cópia da classe e instancia no objeto.
 *
 * Quanto mais inteligente for sua classe, mais limpo será seu código.
 */