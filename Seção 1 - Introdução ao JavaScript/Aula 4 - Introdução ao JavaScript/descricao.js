/**
 * O comando var declara variável.
 * O comando let declara variável definindo o escopo de atuação.
 *     let leva em conta o escopo (aonde foi declarada a variável) em que foi definida.
 * O comando const declara constantes.
 *     const, uma vez declada um valor, ela nunca pode mudar. Afinal, é uma constante e não uma variável.
 *
 * Number - números em JavaScript são do tipo.
 * Variáveis com o mesmo nome e tipo são Array - Armazenando mais de um valor.
 * typeOf - Tipo de dado variável.
 * stanceOf - Tipo de um array.
 *
 * No JavaScript as variáveis são Fracamente tipadas - Tipo de dado variável.
 *     Funções de Conversão parseInt, parseFloat to string.
 *
 * Operadores de comparação:
 *     = Operador de atribuição;
 *     == Compara o valor de duas expressões;
 *     === Compara o valor e o tipo de dados;
 *     != Compara se os valores são diferentes;
 *     !== Compara se os valores e tipos são diferentes;
 *
 * Operadores lógicos:
 *     && (and) Quando duas condições tem que ser verdadeiras ao mesmo tempo;
 *     || (or) Quando uma ou outra condição tem que ser verdeira;
 *     ++ Operador incremental.
 *     -- Operador decremental.
 *
 * if (condição) {instruções}
 * else if (condição) {instruções}
 * else {instruções}
 *
 *     Uma condição If que for verdadeira ANULARÁ as demais condições else if ou else.
 *
 * switch - Estrutura de controle com opções pré-definidas.
 *     let cor = "azul";
 *     switch (cor) {
 *         case "azul":
 *             console.log("A cor é azul!")
 *             break;
 *         defaul:                    // semelhante ao else.
 *             console.log("Não é azul!")
 * break - Utilize sempre o comando para interromper o bloco.
 *
 * !!! diretório - Cuidado como o nome dos arquivos, muitos erros no início são comuns por isso. !!!
 *
 * continue - Similar ao break, porém ignora e vai para o próximo.
 */